apiVersion: apps/v1
kind: Deployment
metadata:
  name: insecure
spec:
  selector:
    matchLabels:
      app: insecure
  template:
    metadata:
      labels:
        app: insecure
    spec:
      containers:
        - name: insecure
          image: nginx:latest
